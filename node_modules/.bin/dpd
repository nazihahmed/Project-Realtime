#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../deployd/bin/dpd" "$@"
  ret=$?
else 
  node  "$basedir/../deployd/bin/dpd" "$@"
  ret=$?
fi
exit $ret
